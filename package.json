{
  "name": "gemini-agent-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm install && cd webapp/server && npm install && cd ../client && npm install",
    "db-setup": "cd webapp/server && npx prisma db push",
    "dev-server": "cd webapp/server && npx nodemon index.js",
    "dev-client": "cd webapp/client && npm run dev",
    "start-app": "npx concurrently \"npm run dev-server\" \"npm run dev-client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
